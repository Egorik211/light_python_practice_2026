06.06.2026
Начало работы
Этап 1: приняли путь к фиксированному параметру запуска всего комплекса проекта.
07.06.2026
Этап 2: Реализовано рекурсивное сканирование папки через os.walk. Для каждого файла собираются метаданные: полный путь, размер в байтах и дата последнего изменения. Размер форматируется в читаемый вид (Б/КБ/МБ/ГБ). Список файлов выводится в консоль построчно, в конце — итоговое количество файлов.
Критерии выполнены: обход папок работает, список файлов выводится. 
PS: изменил сканирование, теперь рекурсивное сканирование папки реализовано вручную без использования os.walk. Функция scan_directory получает список содержимого папки через os.listdir, затем для каждого элемента проверяет: если файл — собирает метаданные (путь, размер, дата изменения), если папка — вызывает себя рекурсивно. 
13.06.2026
Этап 3 — Дубликаты: Реализован поиск дубликатов через MD5-хэш содержимого файлов. Функция find_duplicates проходит по списку файлов, читает каждый и считает хэш через hashlib. Результаты хранятся в словаре хэш -> список путей. В конце выводятся только те группы, где файлов два и больше.
17.06.2026
Этап 4 — Резервная копия: Реализовано сравнение оригинальной папки с бэкапом. Функция check_backup сканирует обе папки и строит два словаря с относительными путями файлов и их размерами. Затем сравнивает их и выводит три категории: файлы отсутствующие в бэкапе, изменённые файлы и лишние файлы в бэкапе. Программа принимает один или два пути — если передан один, запускается только сканирование, если два — добавляется сравнение с бэкапом.
Критерии выполнены: сравнение работает, отсутствующие, изменённые и лишние файлы видны.
21.06.2026
1.Запуск через терминал — программа теперь полноценно запускается командой python src/main.py <путь> без привязки к кнопке PyCharm.
4.Чтение файла по частям — подсчёт хэша в duplicates.py читает файл кусками по 4096 байт вместо загрузки целиком, что экономит память на больших файлах.
4.Фильтрация по расширению — после запуска программа спрашивает через input(), какое расширение оставить (например .py), либо пропустить фильтр нажатием Enter.
4.Разбивка на модули — код вынесен из одного файла в четыре: scanner.py (сканирование и форматирование), duplicates.py (поиск дубликатов), backup.py (сравнение с бэкапом), main.py (точка входа, объединяющая модули).